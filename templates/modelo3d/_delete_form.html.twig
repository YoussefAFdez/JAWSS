<form method="post" action="{{ path('app_modelo3_d_delete', {'id': modelo3_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modelo3_d.id) }}">
    <button class="btn">Delete</button>
</form>
