{% extends 'layout.html.twig' %}

{% block title %}Audio{% endblock %}

{% block contenido %}

    <audio controls class="mt-3 block rounded-xl">
        <source src="{{ vich_uploader_asset(audio) }}">
    </audio>

    <div class="flex items-baseline">
        <h1 class="text-xl my-4 font-bold mr-5">{{ audio.recurso }}</h1>
        <a class="text-blue-600 hover:underline" href="{{ vich_uploader_asset(audio) }}" download><span class="fa-solid fa-download"></span> Descargar</a>
    </div>

    <div class="table-wrapper mb-4">
        <table class="table">
            <thead>
            <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Duración</th>
                <th>Tamaño (MB)</th>
                <th>Extensión</th>
                <th>Propietario</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td class="table-enfasis">{{ audio.recurso.nombre }}</td>
                <td>{{ audio.recurso.descripcion }}</td>
                <td>{{ audio.duracion }}</td>
                <td>{{ (audio.tamanio / 1048576) | number_format(2, ',', '.') }} MB</td>
                <td>{{ audio.recurso.extension }}</td>
                <td class="table-enfasis">
                    <a class="table-link" href="{{ path('app_usuario_show', {'id': audio.recurso.propietario.id}) }}">{{ audio.recurso.propietario }}</a>
                </td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="mb-8">
        <a href="{{ path('app_audio_index') }}" class="text-blue-600 hover:underline mr-2"><span class="fa-solid fa-arrow-left"></span> Volver</a>
    </div>
{% endblock %}
