{% extends 'layout.html.twig' %}

{% block title %}Editar Recurso - {{ parent() }}{% endblock %}

{% block contenido %}
    <h1 class="text-xl my-4 font-bold">Editando "{{ recurso.nombre }}"</h1>

    {{ include('recurso/_form.html.twig', {'button_label': 'Guardar'}) }}
    <div class="mb-4">
        <a href="{{ path('app_recurso_index') }}" class="text-blue-600 mr-2"><span class="fa-solid fa-arrow-left"></span> Volver</a>
        {{ include('recurso/_delete_form.html.twig') }}
    </div>
{% endblock %}
