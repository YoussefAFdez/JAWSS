{% extends 'layout.html.twig' %}

{% block title %}Edit Tier{% endblock %}

{% block contenido %}
    <h1>Edit Tier</h1>

    {{ include('tier/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tier_index') }}">back to list</a>

    {{ include('tier/_delete_form.html.twig') }}
{% endblock %}
