{% extends 'layout.html.twig' %}

{% block title %}Tier{% endblock %}

{% block contenido %}
    <h1>Tier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tier.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tier.nombre }}</td>
            </tr>
            <tr>
                <th>Almacenamiento</th>
                <td>{{ tier.almacenamiento }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tier_index') }}">back to list</a>

    <a href="{{ path('app_tier_edit', {'id': tier.id}) }}">edit</a>

    {{ include('tier/_delete_form.html.twig') }}
{% endblock %}
