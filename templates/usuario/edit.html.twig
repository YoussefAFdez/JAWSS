{% extends 'layout.html.twig' %}

{% block title %}Editar Usuario - {{ parent() }}{% endblock %}

{% block contenido %}
    <h1 class="text-xl my-4 font-bold">Editando "{{ usuario.nombreUsuario }}"</h1>

    {{ include('usuario/_form.html.twig', {'button_label': 'Actualizar'}) }}
    <div class="mb-4">
        <a href="{{ path('app_usuario_index') }}" class="text-blue-600 mr-2"><span class="fa-solid fa-arrow-left"></span> Volver</a>
        {{ include('usuario/_delete_form.html.twig') }}
    </div>
{% endblock %}
